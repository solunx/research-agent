# Task

If a spreadsheet is available under the workspace (e.g. `inputs/` or a path given at run time), open it and report:

- sheet names
- for the first data sheet: header row fields
- row count estimate
- any column that looks like a price or amount (name only)

If **no spreadsheet is found**, say so clearly and stop (do not invent files).

# Output

- Paths tried, sheets, headers, uncertainties
